
const app = require('./index'); // importa a instância do express gofigurada em index.js

app.listen(5000); // inicia o servidor na porta 5000, neste caso a API será acessivel em http://localhost (local)